apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: octohelm
  name: qservice-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: qservice-operator
  template:
    metadata:
      annotations:
        sidecar.istio.io/inject: "false"
      labels:
        name: qservice-operator
    spec:
      serviceAccountName: qservice-operator
      containers:
        - name: qservice-operator
          image: octohelm/qservice-operator:0.2.2
          command:
            - qservice-operator
          imagePullPolicy: Always
          env:
            - name: AUTO_INGRESS_HOSTS
              value: "hw-dev.rktl.xyz"
            - name: WATCH_NAMESPACE
              value: ""
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "qservice-operator"
